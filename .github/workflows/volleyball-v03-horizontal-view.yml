name: Volleyball V0.3 Horizontal View

on:
  push:
    paths:
      - '.github/workflows/volleyball-v03-horizontal-view.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply horizontal top-down volleyball view
        run: |
          python .github/scripts/volleyball_v03_horizontal_view.py
          node --check assets/js/realms/physical-education/sports-minigames.js
          git diff --check
          grep -q 'volleyballWorldToScreen' assets/js/realms/physical-education/sports-minigames.js
          grep -q 'camera top-down horizontal' assets/css/volleyball/volleyball-standalone.css
          grep -q 'volleyball-v03-horizontal' volleyball.html
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/js/realms/physical-education/sports-minigames.js assets/css/volleyball/volleyball-standalone.css volleyball.html game.html
          git commit -m "Volei V0.3: girar quadra para top-down horizontal"
          git push
