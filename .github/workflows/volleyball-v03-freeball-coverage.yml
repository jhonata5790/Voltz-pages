name: Volleyball V0.3 Freeball Coverage

on:
  push:
    paths:
      - '.github/workflows/volleyball-v03-freeball-coverage.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply free ball coverage and rival contact targets
        run: |
          python .github/scripts/volleyball_v03_freeball_coverage.py
          node --check assets/js/realms/physical-education/sports-minigames.js
          git diff --check
          grep -q 'volleyballEnsureVoltzCoverage' assets/js/realms/physical-education/sports-minigames.js
          grep -q 'rivalAttackContact' assets/js/realms/physical-education/sports-minigames.js
          grep -q 'FREE BALL!' assets/js/realms/physical-education/sports-minigames.js
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/js/realms/physical-education/sports-minigames.js
          git commit -m "Volei V0.3: corrigir free ball e cobertura por trajetoria"
          git push
