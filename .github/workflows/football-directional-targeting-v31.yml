name: Football Directional Targeting V3.1

on:
  push:
    paths:
      - '.github/workflows/football-directional-targeting-v31.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply directional targeting patch
        run: |
          python .github/scripts/football_directional_targeting_v31.py
          node --check assets/js/realms/physical-education/sports-minigames.js
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/js/realms/physical-education/sports-minigames.js
          git commit -m "Futebol V3.1: passes direcionais e antecipacao" || exit 0
          git push
